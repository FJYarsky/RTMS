# ==============================================================================
# RTMS — Despliegue Automatizado a GitHub Pages (https://fjyarsky.github.io/RTMS/)
# Desarrollado por Joaquín Yarsky (joaquinyarsky@gmail.com)
# Publica la landing page oficial de RTMS desde la carpeta site/
# ==============================================================================

name: Deploy RTMS Official Website to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "site/**"
      - ".github/workflows/deploy-pages.yml"
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to GitHub Pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: "site"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
        # Despliegue de landing page a GitHub Pages
